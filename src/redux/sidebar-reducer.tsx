import React from "react";
import {ActionTypes, RootStateType} from "./types";

const initialState = {}

export const sidebarReducer = (state = initialState, action:ActionTypes) => {
    return state
}